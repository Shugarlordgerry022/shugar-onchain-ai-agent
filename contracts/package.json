{
  "name": "shugar-onchain-agent-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for Shugar Onchain AI Agent",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bsc_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bsc_mainnet",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "hardhat": "^2.17.0"
  }
}
